import { KeyValue } from './KeyValue';
/**
 * The validator function for the Form component. Expects key-value pair with errors to be returned.
 * The key is the field path, where the value is the error message.
 */
export declare type FormValidatorType = (values: any) => KeyValue<string> | undefined;
